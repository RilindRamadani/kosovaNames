// mapResponseToGraph.ts

import { ApiResponse } from '../types';
import { GraphData } from '../types';

export function mapResponseToGraph(response: ApiResponse): GraphData {
    // Transform the response into the format required by the graph
    // This is just a placeholder. Replace it with your actual transformation logic.
    const graphData: any = {
        // ...
    };

    return graphData;
}